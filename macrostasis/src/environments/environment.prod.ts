/**
 * =============================================================================
 * Environment - Production
 * =============================================================================
 */
export const environment = {
    production: true,

    // WebSocket del backend de ajedrez (Cloudflare)
    chessWebSocketUrl: 'wss://chess.macrostasis.lat',

    // API URLs
    apiUrl: 'https://chess.macrostasis.lat'
};
