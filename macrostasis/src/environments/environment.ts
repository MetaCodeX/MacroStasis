/**
 * =============================================================================
 * Environment - Development (Local)
 * =============================================================================
 */
export const environment = {
    production: false,

    // WebSocket del backend de ajedrez
    chessWebSocketUrl: 'ws://localhost:3081',

    // API URLs (si las necesitas en el futuro)
    apiUrl: 'http://localhost:3081'
};
